//import {seqFiles} from '../files/seqFiles';

export class User {

  id : string;
  name: string;
  userName: string;
  email:string;
  //since the user object is being
  //instantiated when a successful login happens
  //we can take the opportunity to load in the existing work
  //from that user.

  //it should be a variable 'seqFiles' of type 'seqFiles'


}
